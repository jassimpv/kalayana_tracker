<!-- # Flutter Development Rules

- Never run the application.
- Never execute `flutter run`, `dart run`, or launch emulators/simulators.
- Never run any tests.
- Never create, modify, or generate Flutter test files.
- Never execute `flutter test`, `dart test`, integration tests, widget tests, or unit tests.
- Never add test coverage as part of a task.
- Never perform build verification unless explicitly requested.
- Focus only on source code changes and implementation.
- After completing changes, provide a summary of modified files and expected behavior.
- Never uncomment existing commented code.
- Never remove commented code unless explicitly requested.
- Treat commented code as intentionally preserved by the developer.
- Always fix analyze issues.

## Project Preferences

- Use Flutter 3.x and Dart 3.x conventions.
- Follow Clean Architecture where applicable.
- Prefer GetX for state management and navigation.
- Keep code production-ready and maintainable.
- Avoid unnecessary refactoring outside the requested scope.
- Preserve existing UI/UX patterns unless redesign is requested.

## Forbidden commands (do not run)

- `flutter run`
- `flutter test`
- `dart test`
- `flutter build`

Only modify code and explain the changes. Assume the developer will manually verify everything. -->
